<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">



    <item android:title="@string/action_settings">
        <menu>
            <item
                android:id="@+id/menu_noPic"
                android:icon="@drawable/ic_photo"
                android:title="@string/noPic"
                app:actionLayout="@layout/action_picswitch"/>
            <item
                android:id="@+id/menu_night"
                android:icon="@drawable/ic_night"
                android:title="@string/nightMode"
                app:actionLayout="@layout/action_nightswitch"/>
        </menu>
    </item>

    <item android:title="其他">
        <menu>
            <item
                android:id="@+id/menu_cache"
                android:icon="@drawable/ic_cache"
                android:title="清除缓存"
                app:actionLayout="@layout/action_cache"/>
            <item
                android:id="@+id/menu_about"
                android:icon="@drawable/ic_about"
                android:title="关于"/>
            <item
                android:id="@+id/menu_exit"
                android:icon="@drawable/ic_exit"
                android:title="注销登录"/>
        </menu>
    </item>

</menu>
